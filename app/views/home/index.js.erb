$('#my-posts').append('<%= j render partial: "posts/post", collection: @posts %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j paginate @posts %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
